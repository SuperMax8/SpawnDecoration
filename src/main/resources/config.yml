# ModelEngine decoration

# Default render distance of static & tracked decorations
renderRadius: 128

# Use a megcombined.json model in deco gui generated by a script you need to use in your pack
megcombined: false

# DON'T MODIFY
config-version: 2